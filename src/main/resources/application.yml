spring:
  application:
    name: zw-ai-code-mother-backend
  #MySQL
  datasource:
    driver-class-name: com.mysql.cj.jdbc.Driver
    url: jdbc:mysql://localhost:3306/zw_ai_code__mother
    username: root
    password: root
server:
  port: 8123
  servlet:
    context-path: /api
# springdoc-openapi
springdoc:
  group-configs:
    - group: 'default'
      packages-to-scan: com.zw.zwaicodemother.controller
# knife4j
knife4j:
  enable: true
  setting:
    language: zh_cn

